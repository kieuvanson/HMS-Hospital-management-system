// Thêm vào file App.jsx hoặc file router chính
import adminRoutes from "./routes/adminRoutes";
// ...existing code...

// Trong phần định nghĩa route:
// {adminRoutes.map(route => <Route key={route.path} path={route.path} element={route.element} />)}
// ...existing code...
